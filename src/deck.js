const Card = require('./card')

class Deck {
  constructor(card) {
    //this.card = card
  }
  // create an array of unique cards
  // 
  //  returns all cards

  // shuffle deck method {
  //  returns shuffled deck
  // }
}
